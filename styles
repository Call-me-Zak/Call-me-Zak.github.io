/* General Reset */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

/* Body Styling */
body {
    font-family: 'Arial', sans-serif;
    background-color: #0b0e34;
    color: #ffffff;
    line-height: 1.6;
    margin: 0;
    padding: 0;
}

/* Background Video Styling */
.background-video {
    position: fixed; /* Ensures it covers the whole viewport */
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: -1; /* Keeps it behind all content */
}

/* Header Styling */
header {
    background: #1e2152;
    padding: 20px 0;
    text-align: center;
    position: relative; /* Ensures it is above the video */
    z-index: 1; /* Keeps it above the video */
}

header nav ul {
    list-style: none;
    display: flex;
    justify-content: center;
}

header nav ul li {
    margin: 0 15px;
}

header nav ul li a {
    color: #fff;
    text-decoration: none;
    font-size: 18px;
    font-weight: bold;
}

header nav ul li a:hover {
    color: #ff6700;
}

/* Hero Section */
.hero {
    height: 70vh;
    background: url('hero-image.jpg') no-repeat center center/cover;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    position: relative;
    z-index: 1; /* Ensures it is above the video */
}

.hero h1 {
    font-size: 50px;
    margin-bottom: 10px;
    text-transform: uppercase;
}

.hero a {
    display: inline-block;
    margin-top: 20px;
    padding: 10px 20px;
    background: #ff6700;
    color: #fff;
    text-decoration: none;
    font-size: 18px;
    font-weight: bold;
    border-radius: 5px;
}

.hero a:hover {
    background: #e55c00;
}

/* Projects Section */
.projects {
    padding: 60px 20px;
    background: #11144c;
    text-align: center;
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
}

.projects h2 {
    font-size: 36px;
    margin-bottom: 20px;
    text-transform: uppercase;
    width: 100%; /* Make sure the title takes the full width */
}

.projects .project {
    flex: 1 1 22%; /* Ensure four columns fit side by side */
    margin: 10px; /* Space between columns */
    box-sizing: border-box; /* Ensure padding/margins don’t affect total width */
    display: flex;
    flex-direction: column;
    align-items: center;
    transition: background 0.3s ease, box-shadow 0.3s ease, transform 0.3s ease; /* Smooth transition */
    position: relative;
    background: #1e2152; /* Background color for project boxes */
    padding: 20px; /* Padding inside each project box */
    border-radius: 8px; /* Rounded corners */
}

/* Hover Effect */
.projects .project:hover {
    background: rgba(135, 206, 250, 0.3); /* Sky blue color with opacity */
    box-shadow: 0 0 15px rgba(135, 206, 250, 0.7); /* Sky blue glow effect */
    transform: scale(1.05); /* Slightly enlarge on hover */
}

/* Footer */
footer {
    background: #1e2152;
    color: #ffffff;
    text-align: center;
    padding: 20px;
}

footer p {
    margin: 0;
    font-size: 14px;
}

footer p a {
    color: #ff6700;
    text-decoration: none;
}

footer p a:hover {
    text-decoration: underline;
}
